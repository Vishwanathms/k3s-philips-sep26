apiVersion: v1
kind: Service
metadata:
  name: web-alias
  namespace: day02-networking
spec:
  type: ExternalName
  # A DNS-only alias. There is no selector, ClusterIP, or EndpointSlice.
  externalName: web.day02-networking.svc.cluster.local
