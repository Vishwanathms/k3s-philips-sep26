# ex07 · step 3 — a non-pod resource in the same namespace, used only to prove
# the user's Role does NOT extend beyond pods (defense-in-depth check).
# Dummy value only; never store real credentials in a committed manifest.
apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
  namespace: rbac-user-pod-demo
type: Opaque
stringData:
  note: not-a-real-credential
